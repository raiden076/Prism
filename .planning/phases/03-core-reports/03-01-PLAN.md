---
phase: 03-core-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prism-engine/src/routes/whitelist.ts
  - prism-engine/tests/routes/whitelist.test.ts
autonomous: true
requirements: [WHIT-01, WHIT-02, WHIT-03, WHIT-04]

must_haves:
  truths:
    - "Whitelist webhook accepts name, reference_id, phone_number, referrer_phone and returns 201"
    - "User record created with crony role linked to whitelisted source"
    - "Referrer hierarchy established via supervisor_id with depth tracking"
    - "Recursive hierarchy subtree queryable for access control"
    - "Duplicate webhook returns 200 with existing user (idempotent)"
  artifacts:
    - path: "prism-engine/src/routes/whitelist.ts"
      provides: "Whitelist webhook route handler"
      exports: ["whitelistRoutes"]
    - path: "prism-engine/tests/routes/whitelist.test.ts"
      provides: "Whitelist webhook tests"
      contains: "describe"
  key_links:
    - from: "prism-engine/src/routes/whitelist.ts"
      to: "prism-engine/src/lib/queries.ts"
      via: "createUser, getUserByPhone, getUserDescendants"
      pattern: "import.*from.*queries"
    - from: "prism-engine/src/index.ts"
      to: "prism-engine/src/routes/whitelist.ts"
      via: "app.route('/api/v1/whitelist', whitelistRoutes)"
      pattern: "whitelist"
---

<objective>
Whitelist webhook route that creates crony users with hierarchy tracking.

Purpose: Trusted party workers get onboarded via webhook. Hierarchy depth + supervisor chain enables RBAC subtree queries.
Output: Route module + tests, wired to index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prism-engine/src/lib/types.ts
@prism-engine/src/lib/queries.ts
@prism-engine/src/routes/auth.ts
@prism-engine/src/middleware/auth.ts
@prism-engine/src/index.ts
@prism-engine/tests/setup.ts
@prism-engine/tests/factories.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Whitelist webhook route with tests</name>
  <files>prism-engine/src/routes/whitelist.ts, prism-engine/tests/routes/whitelist.test.ts</files>
  <behavior>
    - Test 1: POST /api/v1/whitelist with {name, reference_id, phone_number} creates user + returns 201 with user id, role, phone
    - Test 2: POST with referrer_phone sets supervisor_id to referrer's user id and increments hierarchy_depth
    - Test 3: POST with missing fields returns 400 with error message
    - Test 4: POST with same reference_id twice returns 200 with existing user (idempotent)
    - Test 5: GET /api/v1/hierarchy/subtree/:userId returns descendant user ids via recursive CTE
    - Test 6: Hierarchy subtree for leaf user returns only self
  </behavior>
  <action>
Create `prism-engine/src/routes/whitelist.ts`:

1. Import Hono, Env type, query functions (createUser, getUserByPhone, getUserDescendants, getWhitelistedSourceByReference, createWhitelistedSource) from '../lib/queries'.

2. POST `/` handler (WHIT-01, WHIT-02, WHIT-03):
   - Parse JSON body: { name: string, reference_id: string, phone_number: string, referrer_phone?: string }
   - Validate required fields (name, reference_id, phone_number). Return 400 if missing.
   - Validate phone format E.164 (/^\+\d{7,15}$/). Return 400 if invalid.
   - Check if whitelisted source exists by reference_id (idempotency). If exists, fetch linked user and return 200.
   - If referrer_phone provided: look up referrer user by phone. If found, set supervisor_id = referrer.id, hierarchy_depth = referrer.hierarchy_depth + 1.
   - Create user via createUser with crony role, phone_number, supervisor_id, hierarchy_depth.
   - Create whitelisted source record linking to user with approval_status='approved'.
   - Return 201 { id, role, phone_number, hierarchy_depth, supervisor_id }.

3. GET `/hierarchy/subtree/:userId` handler (WHIT-04):
   - Call getUserDescendants(db, userId).
   - Return 200 { userId, descendants: [{ id, role, phone_number, hierarchy_depth }] }.

4. Export `whitelistRoutes` as new Hono instance.

Wire in index.ts: `app.route('/api/v1/whitelist', whitelistRoutes)` (add alongside existing auth route wiring).

Create `prism-engine/tests/routes/whitelist.test.ts`:
- Use test factories (insertTestUser) for setting up referrer.
- Use applyMigrations for D1 setup.
- Test all 6 behaviors above.
- Run with `bun run test`.
  </action>
  <verify>
    <automated>bun run test -- tests/routes/whitelist.test.ts</automated>
  </verify>
  <done>
Whitelist webhook creates user with hierarchy, handles duplicates, hierarchy subtree queryable. 6 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External -> /api/v1/whitelist | Untrusted input from external party webhook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | POST /whitelist | accept | Webhook source authenticated at infrastructure level (WAF/secret header) -- not in v1 scope |
| T-03-02 | T | phone_number field | mitigate | Validate E.164 format, use parameterized queries |
| T-03-03 | D | reference_id | accept | No sensitive data in reference_id, idempotent lookup is safe |
</threat_model>

<verification>
All whitelist tests pass: `bun run test -- tests/routes/whitelist.test.ts`
</verification>

<success_criteria>
- POST /api/v1/whitelist creates user + whitelisted source, returns 201
- Referrer hierarchy established with supervisor_id + depth tracking
- Duplicate webhook idempotent (200 not 201)
- GET /api/v1/hierarchy/subtree/:userId returns descendants
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-reports/03-01-SUMMARY.md`
</output>
