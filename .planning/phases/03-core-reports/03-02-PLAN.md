---
phase: 03-core-reports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prism-engine/src/routes/reports.ts
  - prism-engine/tests/routes/reports-harvest.test.ts
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, RPT-05]

must_haves:
  truths:
    - "Authenticated whitelisted user submits report with photo stored in R2, geo-tagged with DIGIPIN"
    - "Phase 1 reports auto-set status to 'pending' (whitelist-trusted, skip AI review, enter normal flow)"
    - "Invalid payload (missing media/lat/lon) returns 400 with clear error message"
    - "Photo uploaded to R2 with UUID-based key pattern"
    - "Report includes latitude, longitude, and auto-generated DIGIPIN"
  artifacts:
    - path: "prism-engine/src/routes/reports.ts"
      provides: "Report harvest route handler"
      exports: ["reportRoutes"]
    - path: "prism-engine/tests/routes/reports-harvest.test.ts"
      provides: "Report harvest tests"
      contains: "describe"
  key_links:
    - from: "prism-engine/src/routes/reports.ts"
      to: "prism-engine/src/lib/queries.ts"
      via: "createReport"
      pattern: "import.*createReport"
    - from: "prism-engine/src/routes/reports.ts"
      to: "prism-engine/src/lib/digipin.ts"
      via: "latLngToDIGIPIN (called inside createReport)"
      pattern: "latLngToDIGIPIN"
    - from: "prism-engine/src/routes/reports.ts"
      to: "R2 VAULT binding"
      via: "c.env.VAULT.put(key, body)"
      pattern: "VAULT\\.put"
---

<objective>
Report harvest route for trusted whitelisted users with R2 photo upload and DIGIPIN geo-encoding.

Purpose: Core report ingestion. Whitelisted cronies submit geo-tagged pothole reports with photos. Phase 1 = whitelist-only, reports enter as 'pending' status (skip AI review, trust whitelist), proceed through normal pending->assigned->fixed_pending_verification->resolved flow.
Output: Route module + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prism-engine/src/lib/types.ts
@prism-engine/src/lib/queries.ts
@prism-engine/src/lib/digipin.ts
@prism-engine/src/routes/auth.ts
@prism-engine/src/middleware/auth.ts
@prism-engine/src/index.ts
@prism-engine/tests/setup.ts
@prism-engine/tests/factories.ts

<interfaces>
<!-- ReportStatus from types.ts -- 'approved' does NOT exist in the type system -->
<!-- Valid statuses: pending, pending_review, assigned, fixed_pending_verification, resolved -->
<!-- Checker fix: RPT-04 "auto-approved" interpreted as status='pending'. Whitelist reports skip AI review but enter normal pending->assigned flow. -->
<!-- STATUS_TRANSITIONS: pending -> [pending_review, assigned], assigned -> [fixed_pending_verification], etc. -->

From prism-engine/src/lib/types.ts:
```typescript
export type ReportStatus = 'pending' | 'pending_review' | 'assigned' | 'fixed_pending_verification' | 'resolved';
export const STATUS_TRANSITIONS: Record<ReportStatus, ReportStatus[]> = {
  pending: ['pending_review', 'assigned'],
  pending_review: ['assigned', 'pending'],
  assigned: ['fixed_pending_verification'],
  fixed_pending_verification: ['resolved', 'pending_review'],
  resolved: [],
};
```

From prism-engine/src/lib/queries.ts:
```typescript
export async function createReport(db: D1Database, input: {
  reporterId: string; latitude: number; longitude: number;
  r2ImageUrl: string; status?: ReportStatus;
}): Promise<Report>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report harvest route with R2 upload + validation + tests</name>
  <files>prism-engine/src/routes/reports.ts, prism-engine/tests/routes/reports-harvest.test.ts</files>
  <behavior>
    - Test 1: POST /api/v1/reports/harvest with valid multipart (media, latitude, longitude) + Authorization header returns 201 with report { id, status: 'pending', digipin, r2_image_url }
    - Test 2: POST without Authorization header returns 401
    - Test 3: POST without media returns 400 { error: '...' }
    - Test 4: POST without latitude/longitude returns 400 { error: '...' }
    - Test 5: POST with invalid latitude (out of range) returns 400
    - Test 6: Report DIGIPIN matches expected code for given lat/lon
    - Test 7: Report status is 'pending' (whitelist auto-approve = pending, NOT 'approved' which does not exist in type system)
  </behavior>
  <action>
Create `prism-engine/src/routes/reports.ts`:

1. Import Hono, Env type, createReport from queries.

2. POST `/harvest` handler (RPT-01 through RPT-05):
   - Use withUser() middleware for authentication. Extract user from context.
   - Parse multipart/form-data: extract `media` (File), `latitude` (string), `longitude` (string).
   - Validate: media present, latitude parseable as float in [-90, 90], longitude parseable as float in [-180, 180]. Return 400 with specific error message for each missing/invalid field.
   - Upload photo to R2: key = `harvest/{crypto.randomUUID()}-{media.name}`, body = await media.arrayBuffer().
   - Call createReport with reporterId=user.id, latitude, longitude, r2ImageUrl=`r2://{key}`, status='pending'.
     NOTE: status='pending' per checker fix. ReportStatus type has NO 'approved' value. Whitelist reports enter as pending, skip AI review, proceed through normal pending->assigned flow.
   - Return 201 { id: report.id, status: report.status, digipin: report.digipin, r2_image_url: report.r2ImageUrl, created_at: report.createdAt }.

3. Export `reportRoutes` as new Hono instance.

Wire in index.ts: `app.route('/api/v1/reports', reportRoutes)`.

Create `prism-engine/tests/routes/reports-harvest.test.ts`:
- Use test factories for creating whitelisted user.
- Mock R2 VAULT.put (test environment provides R2 via miniflare).
- Test all 7 behaviors above.
- Use withUser() middleware in test routes (same as production wiring).
  </action>
  <verify>
    <automated>bun run test -- tests/routes/reports-harvest.test.ts</automated>
  </verify>
  <done>
Report harvest route accepts multipart, validates, uploads to R2, creates report with status='pending', DIGIPIN auto-generated. 7 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /api/v1/reports/harvest | Authenticated user uploading multipart data |
| Route handler -> R2 | File storage with UUID key (no user-controlled paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | S | Authorization header | mitigate | withUser() middleware verifies JWT + D1 lookup |
| T-03-05 | T | latitude/longitude | mitigate | Validate range [-90,90]/[-180,180], parameterized queries |
| T-03-06 | I | media upload | mitigate | R2 key uses UUID prefix, no user-controlled path segments |
| T-03-07 | D | R2 object key | accept | UUID-based keys, tamper-evident via DIGIPIN cross-check |
| T-03-08 | E | media size | accept | R2 handles large files; add size limit in future phase if needed |
</threat_model>

<verification>
All harvest tests pass: `bun run test -- tests/routes/reports-harvest.test.ts`
</verification>

<success_criteria>
- Authenticated user submits report with photo via multipart form
- Photo stored in R2 with UUID key
- DIGIPIN auto-generated from lat/lon
- Status set to 'pending' (whitelist-trusted, normal flow entry)
- Invalid payloads return 400 with clear errors
- Missing auth returns 401
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-reports/03-02-SUMMARY.md`
</output>
