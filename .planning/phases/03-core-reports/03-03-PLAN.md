---
phase: 03-core-reports
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - prism-engine/src/routes/reports.ts
  - prism-engine/src/routes/board.ts
  - prism-engine/tests/routes/reports-board.test.ts
  - prism-engine/tests/routes/reports-status.test.ts
autonomous: true
requirements: [RPT-06, RPT-07, RPT-08, RPT-09, RPT-10, RPT-11, RPT-12]

must_haves:
  truths:
    - "Board endpoint returns reports ordered by creation date, paginated, max 100"
    - "Nearby reports queryable by lat/lon/radius with distance calculation"
    - "Reports filterable by status"
    - "Query results scoped by user role (RBAC filter)"
    - "Status state machine rejects invalid transitions"
    - "Valid transitions: pending->assigned, assigned->fixed_pending_verification, fixed_pending_verification->resolved"
  artifacts:
    - path: "prism-engine/src/routes/board.ts"
      provides: "Board query route (v2) + nearby route (v1)"
      exports: ["boardRoutes", "nearbyRoutes"]
    - path: "prism-engine/src/routes/reports.ts"
      provides: "Status transition endpoint (appended to existing reportRoutes)"
      contains: "PATCH"
    - path: "prism-engine/tests/routes/reports-board.test.ts"
      provides: "Board + nearby query tests"
      contains: "describe"
    - path: "prism-engine/tests/routes/reports-status.test.ts"
      provides: "Status transition tests"
      contains: "describe"
  key_links:
    - from: "prism-engine/src/routes/board.ts"
      to: "prism-engine/src/lib/queries.ts"
      via: "getReportsByStatus, getNearbyReports"
      pattern: "import.*queries"
    - from: "prism-engine/src/routes/board.ts"
      to: "prism-engine/src/middleware/rbac.ts"
      via: "getReportsFilter"
      pattern: "import.*getReportsFilter"
    - from: "prism-engine/src/routes/reports.ts"
      to: "prism-engine/src/lib/queries.ts"
      via: "updateReportStatus"
      pattern: "import.*updateReportStatus"
    - from: "prism-engine/src/lib/types.ts"
      to: "status transition"
      via: "isValidTransition, STATUS_TRANSITIONS"
      pattern: "isValidTransition"
---

<objective>
Board query routes (paginated, RBAC-filtered, status-filtered) + nearby reports + status transition endpoint with state machine enforcement.

Purpose: War Room reads report data, field reporters find nearby reports, status machine governs transitions.
Output: Route modules + tests.

**Checker fix applied:** RPT-11 chain updated to match actual ReportStatus type. 'approved' does not exist. Real chain: pending -> assigned -> fixed_pending_verification -> resolved. State machine uses STATUS_TRANSITIONS from types.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prism-engine/src/lib/types.ts
@.planning/phases/03-core-reports/03-01-SUMMARY.md
@.planning/phases/03-core-reports/03-02-SUMMARY.md
@prism-engine/src/lib/queries.ts
@prism-engine/src/middleware/auth.ts
@prism-engine/src/middleware/rbac.ts
@prism-engine/src/routes/reports.ts
@prism-engine/src/index.ts
@prism-engine/tests/setup.ts
@prism-engine/tests/factories.ts

<interfaces>
<!-- Checker fix: RPT-11 transition chain matches actual type system -->
<!-- NO 'approved' state exists. Chain: pending -> assigned -> fixed_pending_verification -> resolved -->

From prism-engine/src/lib/types.ts:
```typescript
export const STATUS_TRANSITIONS: Record<ReportStatus, ReportStatus[]> = {
  pending: ['pending_review', 'assigned'],
  pending_review: ['assigned', 'pending'],
  assigned: ['fixed_pending_verification'],
  fixed_pending_verification: ['resolved', 'pending_review'],
  resolved: [],
};
export function isValidTransition(from: ReportStatus, to: ReportStatus): boolean;
```

From prism-engine/src/lib/queries.ts:
```typescript
export async function getReportsByStatus(db: D1Database, status: ReportStatus, limit?: number): Promise<Report[]>;
export async function getNearbyReports(db: D1Database, latitude: number, longitude: number, radiusMeters: number, limit?: number): Promise<Array<Report & { distanceMeters: number }>>;
export async function updateReportStatus(db: D1Database, reportId: string, newStatus: ReportStatus): Promise<Report | null>;
```

From prism-engine/src/middleware/rbac.ts:
```typescript
export async function getReportsFilter(role: UserRole, userId: string, db: D1Database): Promise<{ whereClause: string; params: string[] }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status transition endpoint + state machine tests</name>
  <files>prism-engine/src/routes/reports.ts, prism-engine/tests/routes/reports-status.test.ts</files>
  <behavior>
    - Test 1: PATCH /api/v1/reports/:id/status with { status: 'assigned' } on pending report returns 200 with updated status
    - Test 2: PATCH with invalid transition (pending -> resolved) returns 400 { error: 'Invalid transition...' }
    - Test 3: PATCH on non-existent report returns 404
    - Test 4: Full valid chain: pending -> assigned -> fixed_pending_verification -> resolved (each step returns 200)
    - Test 5: Transition from resolved to any state returns 400 (resolved is terminal)
    - Test 6: PATCH without auth returns 401
  </behavior>
  <action>
Append to existing `prism-engine/src/routes/reports.ts`:

1. PATCH `/:id/status` handler (RPT-10, RPT-11, RPT-12):
   - Use withUser() middleware. Require role admin or contractor via requireRole('admin', 'contractor').
   - Parse JSON body: { status: string }.
   - Validate status is valid ReportStatus value. Return 400 if not.
   - Call updateReportStatus(db, reportId, newStatus).
     updateReportStatus internally calls isValidTransition() from types.ts. Returns null if invalid transition or report not found.
   - If null: return 404 if report not found, or 400 { error: `Invalid transition from ${current} to ${newStatus}` } if transition blocked.
   - Return 200 { id, status, updated_at }.

NOTE on RPT-11: The requirement says "pending -> approved -> assigned -> fixed_pending_verification -> resolved" but 'approved' does NOT exist in ReportStatus type. The actual valid chain is: pending -> assigned -> fixed_pending_verification -> resolved. Whitelist reports enter as 'pending' and proceed directly to 'assigned' without an intermediate 'approved' step. This avoids migration + type changes.

Create `prism-engine/tests/routes/reports-status.test.ts`:
- Use test factories for user + report creation.
- Test all 6 behaviors above.
- Verify state machine enforces STATUS_TRANSITIONS from types.ts.
  </action>
  <verify>
    <automated>bun run test -- tests/routes/reports-status.test.ts</automated>
  </verify>
  <done>
Status transition endpoint enforces state machine. Valid chain: pending->assigned->fixed_pending_verification->resolved. Invalid transitions rejected. 6 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Board query + nearby routes with RBAC filtering + tests</name>
  <files>prism-engine/src/routes/board.ts, prism-engine/tests/routes/reports-board.test.ts, prism-engine/src/index.ts</files>
  <behavior>
    - Test 1: GET /api/v2/reports returns reports ordered by created_at DESC, max 100
    - Test 2: GET /api/v2/reports?status=pending returns only pending reports
    - Test 3: GET /api/v2/reports with admin auth returns all reports (RBAC filter 1=1)
    - Test 4: GET /api/v2/reports with crony auth returns only own reports + verified
    - Test 5: GET /api/v1/reports/nearby?lat=28.6&lon=77.2&radius=1000 returns reports within radius with distance
    - Test 6: GET /api/v1/reports/nearby without lat/lon returns 400
  </behavior>
  <action>
Create `prism-engine/src/routes/board.ts`:

1. GET `/` handler -- board endpoint (RPT-06, RPT-08, RPT-09):
   - Use withUser() middleware for RBAC filtering.
   - Parse query params: status (optional ReportStatus filter), limit (optional, default 100, max 100).
   - Get user from context, call getReportsFilter(user.role, user.id, db) for WHERE clause.
   - Query: `SELECT * FROM Reports WHERE ${filter.whereClause} ${statusFilter} ORDER BY created_at DESC LIMIT ?` with bound params.
   - Return 200 { reports: Report[], count, filter: { role, status } }.

2. Export `boardRoutes` as new Hono instance.

**Checker fix: Route wiring split (warning fix).**
Wire in index.ts:
- `app.route('/api/v2/reports', boardRoutes)` -- board query at v2 prefix
- Nearby route stays under reportRoutes at v1 prefix: GET `/nearby` on reportRoutes (already wired as `/api/v1/reports`)

Add GET `/nearby` to `prism-engine/src/routes/reports.ts` (RPT-07):
- Parse query params: lat (required), lon (required), radius (optional, default 200m).
- Validate lat/lon present and numeric. Return 400 if missing.
- Call getNearbyReports(db, lat, lon, radius).
- Return 200 { reports: Array<Report & { distanceMeters }>, count }.

Create `prism-engine/tests/routes/reports-board.test.ts`:
- Use test factories for users + reports with various statuses.
- Test all 6 behaviors above.
- For RBAC tests: create users with different roles, verify filtered results.
  </action>
  <verify>
    <automated>bun run test -- tests/routes/reports-board.test.ts</automated>
  </verify>
  <done>
Board endpoint returns paginated, RBAC-filtered reports. Nearby route returns distance-sorted reports. Status filter works. Only /nearby under v1, board under v2. 6 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /api/v2/reports | Authenticated user querying reports |
| Client -> /api/v1/reports/nearby | Public endpoint (no auth required for nearby) |
| Client -> PATCH /:id/status | Authenticated admin/contractor changing status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | E | board query limit | mitigate | Cap at 100 results max, reject > 100 |
| T-03-10 | I | status transition | mitigate | withUser + requireRole(admin, contractor), isValidTransition enforces state machine |
| T-03-11 | T | nearby lat/lon | mitigate | Validate numeric, range check [-90,90]/[-180,180] |
| T-03-12 | I | RBAC filter | mitigate | getReportsFilter generates parameterized WHERE, no string interpolation |
</threat_model>

<verification>
All board + status tests pass:
```
bun run test -- tests/routes/reports-board.test.ts
bun run test -- tests/routes/reports-status.test.ts
```
</verification>

<success_criteria>
- GET /api/v2/reports returns paginated reports with RBAC filtering
- GET /api/v1/reports/nearby returns distance-sorted nearby reports
- Status filter on board endpoint works
- PATCH /api/v1/reports/:id/status enforces state machine (pending->assigned->fixed_pending_verification->resolved)
- Invalid transitions rejected with 400
- Board routes under /api/v2, only nearby under /api/v1 (per checker warning fix)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-reports/03-03-SUMMARY.md`
</output>
